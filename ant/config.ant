<?xml version="1.0" encoding="UTF-8" ?>

<!-- the project global configuration for the build system.
 
 Please: be very careful when modifing this, as this file lives in GIT and you can easily break
 other peoples system when commiting changes to this file. -->

<project name="config">
	
	<!-- =================================================================== -->
	<!-- DIRECTORY CONFIGURATION -->
	
	<!-- root directory of the common source tree -->
	<property name="src.home" value="src" />
	
	<!-- directory that contains all libraries that are required in the .war -->
	<property name="lib.home" value="lib" />
	
	<!-- build directory that will contain all the .jars and what else is required for a .war -->
	<property name="build.home" value="build" />
	
	<!-- build directory that contains all files to include in the .zips except for the .jars -->
	<property name="file.home" value="files" />
	
	<!-- directory that contains packed archives, such as the .war -->
	<property name="dist.home" value="dist" />
	
	
	<!-- path to the .jar file that shall be created -->
	<property name="jar.ims" value="${dist.home}/Components/GgServerIMS.jar" />
	<property name="jar.elsims" value="${dist.home}/Components/GgServerELSData/GgServerElsIMS.jar" />
	
	<!-- path to the .jar file that shall be created -->
	<property name="jar.imr" value="${dist.home}/Components/GgServerIMR.jar" />
	
	<!-- path to the .jar file that shall be created -->
	<property name="jar.ime" value="${dist.home}/Components/GgServerIME.jar" />
	
	<!-- path to the .jar file that shall be created -->
	<property name="jar.imi" value="${dist.home}/Components/GgServerIMI.jar" />
	<property name="jar.imipdf" value="${dist.home}/Components/GgServerIMIData/PdfImporter.jar" />
	<property name="jar.imipdfslave" value="${dist.home}/Components/GgServerIMIData/PdfImporterData/PdfImporterSlave.jar" />
	
	<!-- path to the .jar file that shall be created -->
	<property name="jar.imipdf" value="${dist.home}/Components/GgServerIMIData/PdfImporter.jar" />
	
	<!-- path to the .jar file that shall be created -->
	<property name="jar.imp" value="${dist.home}/Components/GgServerIMP.jar" />
	<property name="jar.impslave" value="${dist.home}/Components/GgServerIMPData/GgServerImpSlave.jar" />
	
	
	<!-- path to the .jar file that shall be created -->
	<property name="jar.imsdocio" value="${dist.home}/Plugins/ImsDocumentIO.jar" />
	
	<!-- path to the .jar file that shall be created -->
	<property name="jar.imeclt" value="${dist.home}/Plugins/GgImagineQcToolImeDataBase.jar" />
	
	
	<!-- path to the application .zip file that shall be created -->
	<property name="pack.serverim" value="${dist.home}/GgServer.im.zip" />
</project>